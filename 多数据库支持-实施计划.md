# 多数据库支持 · 实施计划

> 文档版本：v0.2 · 时间：2026-08-15（v0.2 已落地 P0-1 架构 + MySQL 族端到端验证）
> 状态：P0-1 已实现，MySQL 族已用本机 MySQL 8.0.37 端到端验证
> 决策记录（用户 2026-08-15）：
> - 目标清单：**7 国际库 + 3 国产库**（MySQL / MariaDB / PostgreSQL / SQLite / Oracle / SQL Server / MongoDB + TiDB / OceanBase / openGauss），共 10 种；达梦 DM、KingbaseES 列入 backlog。
> - 验证环境：本机有 **MySQL(8.0.37, root/123456) + PostgreSQL(5432)**，MySQL 族已真实验证；Oracle/MSSQL/Mongo 需联网装驱动，代码就绪后另验。
> - 执行顺序：**先做 P0-1 架构**（协议族复用 + 每库预设体系 + 前端向导多类型），再逐个库连通。
>
> **v0.2 落地记录**：types.ts 扩展 12 型 + presets/ 模块（10 预设+2 backlog）+ 注册表协议族化 + preview-server 按协议族分派（MySqlDriver 桥接 $N→? 与双引号→反引号）+ 前端向导 10 类型动态表单 + 能力矩阵禁用。MySQL 端到端全通：连接/元数据/表结构/参数化增删查/导出 SQL/跨库传输(MySQL→SQLite)。运行方式：`bash run-preview.sh`（mysql2 预装于 C:\Users\Darker\.dbadmin-deps，NODE_PATH 指向）。

---

## 一、总体设计：协议族复用 + 每库预设

```
前端 / 连接向导（类型选择 · 表单字段按库切换 · 能力开关）
        │
方言注册表（协议族映射）
  ├─ MySQL 族  mysql2 驱动 → mysql / mariadb / tidb / oceanbase
  ├─ PG 族     pg 驱动     → postgres / openGauss
  ├─ 独立驱动              → sqlite(node:sqlite) / oracle(oracledb) / mssql(mssql) / mongo(mongodb)
  └─ backlog               → dm(达梦) / kingbase
        │
每库预设（8 类，新库 = 复用协议族 + 填一张预设表）
默认端口 · 认证方式 · 标识符/占位符 · 元数据 SQL · DDL 类型映射 · 分页 · 工具命令 · 能力矩阵
```

核心原则：**驱动按协议族共享，差异全部收敛到预设表**。新增 TiDB = mysql 族驱动 + 一张预设；元数据 SQL 允许按库覆盖（openGauss 的系统视图与 PG 有出入）。

---

## 二、目标清单与协议族归属

| # | 数据库 | 协议族 | 驱动 | 默认端口 | 接入成本 | 备注 |
|---|---|---|---|---|---|---|
| 1 | MySQL | mysql | mysql2 | 3306 | 已就绪待跑通 | 反引号引用 |
| 2 | MariaDB | mysql | mysql2 | 3306 | 已就绪待跑通 | 预设标注版本差异 |
| 3 | TiDB | mysql | mysql2 | 4000 | 低 | 同 MySQL，分布式特性 |
| 4 | OceanBase | mysql | mysql2 | 2881 | 低 | MySQL 兼容模式 |
| 5 | PostgreSQL | pg | pg | 5432 | ✅ 已验证 | 基准实现 |
| 6 | openGauss | pg | pg | 5432 | 低 | 系统视图微调 + 隐藏 gs_* 对象 |
| 7 | SQLite | 独立 | node:sqlite | 文件 | ✅ 已验证 | 单文件库 |
| 8 | Oracle | 独立 | oracledb | 1521 | 中 | SID/ServiceName；需预编译二进制 |
| 9 | SQL Server | 独立 | mssql | 1433 | 中 | 实例名 / Windows 集成认证 |
| 10 | MongoDB | 独立 | mongodb | 27017 | 中 | 无 SQL，单独对象树/查询逻辑 |
| — | 达梦 DM | 独立 | 待评估 | 5236 | 高 | backlog |
| — | KingbaseES | pg 族 | pg | 54321 | 低 | backlog（协议兼容） |

---

## 三、每库预设数据模型（presets/ 模块）

```ts
// types.ts 扩展
export type DbType = 'mysql' | 'mariadb' | 'tidb' | 'oceanbase'
  | 'postgres' | 'openGauss' | 'sqlite' | 'oracle' | 'mssql' | 'mongo'
  | 'dm' | 'kingbase';                     // 后两个先注册不进 UI

export type ProtocolFamily = 'mysql' | 'pg' | 'sqlite' | 'oracle' | 'mssql' | 'mongo';

export interface DbPreset {
  type: DbType;
  family: ProtocolFamily;       // 驱动复用依据
  label: string;                // 前端显示名
  defaultPort?: number;
  connectFields: ConnectField[];// 连接表单字段（host/port/user/pwd/db/sid/serviceName/instance/file…）
  quote: { identifier: string; string: string };   // 标识符/字符串引用符
  paramStyle: 'dollar' | 'question' | 'colon' | 'at'; // 参数占位符
  pagination: string;           // 分页模板（LIMIT $n / OFFSET…）
  autoIncrement: string;        // 自增写法说明
  // 元数据查询（可覆盖，family 有默认实现）
  sql?: { listDatabases?: string; listTables?: string; describeTable?: string };
  hiddenSystemSchemas?: string[];  // 对象树要隐藏的 system schema/库
  treeShape: 'db-table' | 'db-schema-table' | 'db-user-table' | 'collection'; // 对象树层级
  tools?: { dump?: string };    // 备份工具命令
  capabilities: {               // 能力矩阵 → 前端禁用按钮
    sqlEditor: boolean; design: boolean; importExport: boolean;
    backup: boolean; userMgmt: boolean; monitor: boolean; er: boolean;
    structureSync: boolean; dataSync: boolean;
  };
}
```

新增目录 `server/src/connection/presets/`：
- `base.ts`（通用字段定义）
- `mysql-family.ts` / `pg-family.ts`（族默认实现：元数据 SQL、quote、param、分页）
- 每库一张预设：`mysql.ts / mariadb.ts / tidb.ts / oceanbase.ts / postgres.ts / openGauss.ts / sqlite.ts / oracle.ts / mssql.ts / mongo.ts`
- `index.ts`（`getPreset(type)` + `allPresets()`）

---

## 四、每库预设速查（10 库关键差异）

| 库 | 引用符 | 占位符 | 分页 | 自增 | 元数据来源 | 树层级 | 隐藏对象 |
|---|---|---|---|---|---|---|---|
| MySQL | `` ` `` | `?` | LIMIT | AUTO_INCREMENT | information_schema | db→table | mysql/sys 库 |
| MariaDB | `` ` `` | `?` | LIMIT | AUTO_INCREMENT | information_schema | db→table | mysql/sys 库 |
| TiDB | `` ` `` | `?` | LIMIT | AUTO_INCREMENT | information_schema | db→table | mysql/sys 库 |
| OceanBase | `` ` `` | `?` | LIMIT | AUTO_INCREMENT | information_schema | db→table | mysql/sys 库 |
| PostgreSQL | `"` | `$1` | LIMIT/OFFSET | GENERATED IDENTITY | information_schema + pg_catalog | db→schema→table | pg_catalog/information_schema |
| openGauss | `"` | `$1` | LIMIT/OFFSET | GENERATED IDENTITY | 同 PG（部分视图差异） | db→schema→table | pg_catalog/information_schema + **gs_\*** |
| SQLite | `"`/`'` | `?` | LIMIT | AUTOINCREMENT | sqlite_master | file→table | sqlite_* |
| Oracle | `"` | `:name` | ROWNUM/OFFSET FETCH | GENERATED AS IDENTITY | ALL_* 视图 | db→user→table | SYS/SYSTEM 用户 |
| SQL Server | `[]` | `@p` | TOP/OFFSET FETCH | IDENTITY | sys.* | db→schema→table | sys/sysdatabases |
| MongoDB | — | — | — | — | listCollections | db→collection | admin/local |

能力矩阵（默认推导，个别覆盖）：
- SQLite：无 userMgmt / monitor / structureSync（ALTER 受限，设计器只读结构）
- MongoDB：仅 sqlEditor(JSON 查询) / importExport / er 弱；其余全部关闭
- Oracle/MSSQL：全开（备份走各自工具，P1 补）

---

## 五、改造点清单（按文件）

1. **`server/src/connection/dialects/types.ts`**：DbType 扩展 + ProtocolFamily + DbPreset 接口
2. **`server/src/connection/dialects/index.ts`**：注册表改为「协议族 → 驱动工厂」+「type → preset」双层；`createDialect` 校验 type 与 family
3. **`server/src/connection/presets/`**（新建）：族默认实现 + 10 张预设表
4. **`server/src/connection/dialects/mysql.ts / postgres.ts`**：元数据 SQL 改为读 preset 的 sql 覆盖（默认用族实现）
5. **`server/preview-server.cjs`**（当前唯一可运行载体，先改）：连接分支从 if/else 改为 preset 驱动；新增 `mysql` 协议（连本机 MySQL）；openGauss 走 pg 协议
6. **`web/preview/index.html`**：连接向导类型下拉扩到 10 种；表单字段按 preset 动态渲染；默认端口自动填；能力矩阵禁用按钮
7. **`web/src/types.ts` + `ConnectionModal.tsx` + `ConnectionManager.tsx`**：React 工程同步（后置，P0-2/3）
8. **`server/src/connection/dto.ts / connection.service.ts`**：type 白名单校验从 8 型扩到 10 型

---

## 六、分阶段里程碑（含验证点）

### P0-1 架构与两大协议族跑通（本机可验证）
- [ ] types.ts 扩展 + presets/ 模块 + 注册表重构
- [ ] preview-server.cjs 接入 MySQL 协议族（mysql/mariadb/tidb/oceanbase 共用一套查询实现，仅 preset 不同）
- [ ] preview 接入 openGauss 预设（复用 pg 驱动，隐藏 gs_*）
- [ ] 前端向导 10 类型 + 动态表单 + 默认端口 + 能力开关
- **验证**：本机 MySQL + PG 双端到端（连接/对象树/查询/数据编辑/设计器），TiDB/OceanBase/openGauss 无环境则预设就绪 + 类型可选

### P0-2 独立驱动真实实现（需联网 npm install）
- [ ] Oracle：oracledb + SID/ServiceName 表单 + ALL_* 元数据
- [ ] SQL Server：mssql + 实例名/Windows 认证 + sys.* 元数据
- [ ] MongoDB：mongodb + 集合/文档浏览 + JSON 查询（对象树单独逻辑）
- **验证**：有环境则端到端；无环境则单元级验证元数据 SQL 构造

### P0-3 预设深化
- [ ] React 工程（web/src）全量同步 10 类型
- [ ] 各库备份/导入导出/用户管理/监控按能力矩阵补齐（含 mysqldump / pg_dump 参数白名单）

### Backlog
- [ ] 达梦 DM（驱动评估：官方 Node 驱动 / JDBC bridge）
- [ ] KingbaseES（pg 族，54321，仅预设）
- [ ] 结构同步 / 数据同步 / ER 多库支持（原路线图 P1/P2）

---

## 七、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| oracledb 需预编译二进制 | 离线/沙箱装不上 | P0-2 联网机器验证；代码层先就绪 |
| openGauss 系统视图与 PG 有差异 | 对象树可能缺对象 | 预设 sql 覆盖机制兜底；无环境时留 TODO 标注 |
| Mongo 无 SQL 方言 | 不能复用关系库逻辑 | 独立 adapter + 对象树单独实现，能力矩阵收敛 |
| MariaDB/TiDB/OceanBase 特性差异 | 误用 MySQL 特有语法 | 预设标注差异项（JSON 类型、分区、ALTER 限制） |
| 10 库连接表单字段差异大 | 前端表单复杂 | ConnectField 声明式驱动渲染，不写死 |

---

## 八、验收标准

1. 连接向导可选 10 种数据库，表单字段/默认端口/能力开关随类型正确切换
2. 本机 MySQL、PostgreSQL 端到端可用（连接→对象树→查询→编辑→设计器）
3. 新增一种协议兼容库（如 KingbaseES）只需加一张预设表，无需改驱动/前端
4. `supportedTypes()` 返回 10 型；前端不暴露 backlog 类型
